# Секретный ключ hCaptcha для серверной валидации токенов
# Получить на https://dashboard.hcaptcha.com/
HCAPTCHA_SECRET=

# Соль для генерации хешей действий API (должна совпадать между фронтом и BFF)
# Если не задана — генерируется автоматически при каждой сборке
BUILD_SALT=

# Секрет для подписи запросов (должен совпадать между фронтом и BFF)
# Если не задан — генерируется автоматически при каждой сборке
SIGNING_SECRET=

# Целевой API-сервер (для BFF-прокси)
API_TARGET=https://api.relics.pw-hub.ru

# URL сайта (для OG-тегов)
SITE_URL=https://relics.pw-hub.ru

# Порт BFF-сервера
PORT=3000
